config:
  target: "https://beta.azureworkbench.com"
  phases:
    - duration: 20
      arrivalRate: 10
scenarios:
  - flow:
      - get:
          url: "api/dia/qs"
          qs:
            category: "WebApp"
            name: "NTier-ASE"